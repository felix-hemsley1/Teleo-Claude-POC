# Cross-platform subset of requirements.txt, installable on Linux/macOS for
# developing and testing the non-capture parts of Teleo (storage, pipeline,
# generation, UI). The Windows-only capture deps live in requirements.txt.
anthropic>=0.40.0
voyageai>=0.2.0
numpy>=1.26.0
pandas>=2.1.0
playwright>=1.45.0
streamlit>=1.32.0
apscheduler>=3.10.0
sqlite-utils>=3.36
python-dotenv>=1.0.0
pydantic>=2.5.0
